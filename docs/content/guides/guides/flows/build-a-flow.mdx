---
title: Build a Sign-In Flow
sidebar_position: 4
persona: iam
description: Build a working username and password sign-in flow with the <ProductName /> Flow Designer — step by step.
---

# Build a Sign-in Flow

By the end of this guide you will have a working username and password sign-in flow assigned to your application.

## Prerequisites

- <ProductName /> is running. See [Get Started](../../getting-started/get-thunderid).
- You have created an application. See [Quickstart](../../quick-start/quickstart).

<Stepper>

## Open the Flow Designer

1. Sign in to the <ProductName /> Console at [https://localhost:8090/console](https://localhost:8090/console).
2. Go to **Flows** → **+ Create New Flow**.
3. Select **Sign-in** as the flow type and click **Continue**.
4. Select the **Basic** template and click **Continue**.
5. Give your flow a name — for example, `Sign In Flow`.

The canvas opens with a pre-wired flow: a **Sign In** view node → **Identifier + Password** executor → **Authorization** executor → **Auth Assertion Generator** executor → **END**. This is exactly the flow you need for username and password authentication.

:::tip You're almost done
The default flow is a complete, working username and password sign-in flow. If that's all you need, skip to [Assign the flow to your application](#assign-the-flow-to-your-application).
:::

## Review the Canvas

The default flow has three nodes:

| Node | What it does |
|---|---|
| **Sign In** (View) | Renders the sign-in form with username and password fields |
| **Identifier + Password** (Executor) | Validates the submitted credentials against the user store |
| **Authorization** (Executor) | Evaluates authorization policies for the authenticated user |
| **Auth Assertion Generator** (Executor) | Issues the authentication assertion on success |

Each node connects to the next via its **green (success) output**. The final executor connects to **END**.

:::note
Every executor also has a **red (failure) output**. In the default flow this loops back to the Sign In view, so users see an error and can retry. Don't leave failure paths unconnected — an unconnected failure path stops the flow with an error.
:::

## Save the Flow

Click **Save** in the top-right corner. <ProductName /> saves the flow and assigns it a version number.

## Assign the Flow to Your Application

A flow only activates once assigned to an application.

1. In the Console, go to **Applications** and open the application you created in the Quickstart.
2. Open the **Flows** tab.
3. Under **Authentication Flow**, select the flow you just saved from the dropdown.
4. Click **Save**.

<ProductName /> will now run this flow whenever a user signs in to your application.

</Stepper>

## What's Next

Your sign-in flow is live. The next step is connecting your app so it can redirect users through this flow.

<div style={{display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(260px, 1fr))', gap: '1rem', marginTop: '1.5rem'}}>

  <a href="../../../quick-start/connect-your-application/react" style={{textDecoration: 'none', color: 'inherit'}}>
    <div style={{border: '1px solid var(--ifm-color-emphasis-300)', borderRadius: '10px', padding: '1.25rem', cursor: 'pointer'}}>
      <div style={{fontSize: '1rem', fontWeight: '700', marginBottom: '0.4rem'}}>Step 4 → Connect your React app</div>
      <div style={{fontSize: '0.875rem', color: 'var(--ifm-color-emphasis-700)', marginBottom: '0.75rem'}}>Install the Asgardeo React SDK, configure your Client ID, and add working sign-in in minutes.</div>
      <div style={{fontSize: '0.875rem', color: 'var(--ifm-color-primary)', fontWeight: 600}}>React quickstart →</div>
    </div>
  </a>

</div>

## Go Further

Once you have the baseline working, the Flow Designer lets you add steps to your flow without changing your application code.

<details>
  <summary>Add multi-factor authentication (MFA)</summary>

1. From the <ProductName /> Console, go to **Applications** and open the application you want to configure.
2. Select the **Flows** tab.
3. Under **Authentication Flow**, **Registration Flow**, or **Recovery Flow**, choose the flow you created from the dropdown.
4. Click **Save**.

<ProductName /> executes the assigned flow when a user initiates the corresponding action (sign in, sign up, or password recovery).

:::note
For **Recovery Flow**, toggle the switch to enable password recovery before selecting a flow.
:::

</details>

<details>
  <summary>Add passkey authentication</summary>

  1. In the left panel under **Widgets**, click **+** next to **Passkey**.
  2. Wire the passkey executor between your Sign In view and the Auth Assertion Generator.
  3. To offer passkeys alongside password, connect the Sign In view to both executors and merge the success paths.
  4. Save and reassign to your application.

</details>

<details>
  <summary>Add social login</summary>

  1. Configure a social identity provider first — currently only supported via API.
  3. Wire it in parallel with or as an alternative to the **Identifier + Password** executor.
  4. Save.

</details>

<details>
  <summary>Build from scratch</summary>


  1. Click **Flows** → **+ Create New Flow**.
  2. Select a flow type: **Sign-in**, **Self Sign-up**, or **Recovery**.
  3. Select **+ Start from scratch** and enter a flow name.
  4. A canvas with START → Blank View → END nodes appears.
  5. Add nodes from **Widgets** (pre-wired View + Executor pairs) or **Steps** (View nodes you wire manually).
  6. Connect nodes by dragging from an output port to an input port.

</details>

<details>
  <summary>Build and configure a registration flow</summary>

  A registration flow controls how new users sign up for your application. To create one:

  1. In the Console, go to **Flows** → **+ Create New Flow**.
  2. Select **Self Sign-up** as the flow type and click **Continue**.
  3. Choose a template — for example, **Basic** ("Username, password, and profile info registration") — or **Blank** to start from scratch.
  4. Give the flow a name and click **Create**.
  5. The canvas opens with the selected template pre-wired. Customize the steps as needed, then click **Save**.

  To assign the registration flow to your application:

  1. Go to **Applications** and open the application.
  2. Open the **Flows** tab and find the **Registration Flow** section.
  3. Enable the toggle to activate self sign-up for the application.
  4. Select the flow you created from the dropdown.
  5. Click **Save**.

  <ProductName /> will now run the selected flow when a user initiates sign-up for your application.

</details>

<details>
  <summary>Build and configure a recovery flow</summary>

  A recovery flow lets users regain access to their account — for example, via email-based password reset. To create one:

  1. In the Console, go to **Flows** → **+ Create New Flow**.
  2. Select **Password Recovery** as the flow type and click **Continue**.
  3. Choose a template — for example, **Email Link** ("Email-based password recovery with email link") or **SMS OTP** — or **Blank** to start from scratch.
  4. Give the flow a name and click **Create**.
  5. Review and customise the canvas, then click **Save**.

  To assign the recovery flow to your application:

  1. Go to **Applications** and open the application.
  2. Open the **Flows** tab and find the **Recovery Flow** section.
  3. Enable the toggle to activate recovery for the application.
  4. Select the flow you created from the dropdown.
  5. Click **Save**.

  <ProductName /> will now run the selected flow when a user requests password or account recovery.

</details>

## Reference

- [Flow Concepts](./flow-concepts) — Node types, left panel sections, and canvas connection rules. 
- [Executors](./executors-reference) - Every executor, its required inputs, and its outputs.
- [Flow Reference](./flow-reference) — Tips for flow building.
- [Password Recovery Setup](../password-recovery-setup) - Set up password recovery for your application.
