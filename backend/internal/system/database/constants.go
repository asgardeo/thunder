// Package database contains key components related to database operations
package database

const (
	// UserDBClientName is the db client name corresponding to the user datasource
	UserDBClientName = "user"
	// ConfigDBClientName is the db client name corresponding to the user datasource
	ConfigDBClientName = "config"
	// RuntimeDBClientName is the db client name corresponding to the user datasource
	RuntimeDBClientName = "runtime"
)
